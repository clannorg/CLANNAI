# CLANNAI Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Gemini AI API Key (required for AI analysis features)
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (for web apps)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clannai_dev
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# Alternative: AWS RDS Database URL (comment out if using individual DB settings above)
# DATABASE_URL=postgresql://username:password@your-rds-endpoint.amazonaws.com:5432/dbname

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# AWS Configuration (for cloud features)
AWS_REGION=eu-west-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Environment
NODE_ENV=development
DEBUG=true

# Server Configuration
PORT=3002

# Frontend API URLs
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002
NEXT_PUBLIC_NO_AUTH_API_BASE_URL=http://localhost:3002

# AWS Cognito (for authentication)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=your_cognito_user_pool_id_here
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=your_cognito_client_id_here
NEXT_PUBLIC_AWS_REGION=eu-west-1

# VEO Integration (for video analysis)
VEO_COOKIE=your_veo_cookie_here